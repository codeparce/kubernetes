apiVersion: v1
kind: Service
metadata:
  name: ${NAME}-service
  namespace: ${NAMESPACE}
spec:
  selector:
    app: ${NAME}
  ports:
    - name: http       
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443

  type: ClusterIP